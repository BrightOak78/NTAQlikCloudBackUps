///$tab Main
SET ThousandSep=',';
SET DecimalSep='.';
SET MoneyThousandSep=',';
SET MoneyDecimalSep='.';
SET MoneyFormat='$#,##0.00;-$#,##0.00';
SET TimeFormat='h:mm:ss TT';
SET DateFormat='DD/MM/YYYY';
SET TimestampFormat='DD/MM/YYYY h:mm:ss[.fff] TT';
SET FirstWeekDay=6;
SET BrokenWeeks=1;
SET ReferenceDay=0;
SET FirstMonthOfYear=1;
SET CollationLocale='en-US';
SET CreateSearchIndexOnReload=1;
SET MonthNames='Jan;Feb;Mar;Apr;May;Jun;Jul;Aug;Sep;Oct;Nov;Dec';
SET LongMonthNames='January;February;March;April;May;June;July;August;September;October;November;December';
SET DayNames='Mon;Tue;Wed;Thu;Fri;Sat;Sun';
SET LongDayNames='Monday;Tuesday;Wednesday;Thursday;Friday;Saturday;Sunday';
SET NumericalAbbreviation='3:k;6:M;9:G;12:T;15:P;18:E;21:Z;24:Y;-3:m;-6:μ;-9:n;-12:p;-15:f;-18:a;-21:z;-24:y';



LET vToday = Floor(Today(1));


///$tab Dim
Areas:
LOAD
    Area_Name,
    City_Type_Group,
    HH,
    Total_pop,
    AVG_PPHH,
    Madad_Points,
    G_Madad,
    Fut_Madad,
    migzar,
    LI_Families,
    LI_Singles,
    LI_Adults,
    LI_Matrix_Type,
    Tech,
    Tech_ch,
    HH_Dlv_2,
    HH_Dlv_5,
    Customers,
    Pidion,
    Avg_Pidion_HH,
    Avg_Pidion_cust,
    Slc_EZ_cust_hh,
    Model_Pidion,
    קטגוריה,
    Model_GR,
    Geometry,
    MapStat_Val,
    Points_Id,
    עיר,
    City
FROM [lib://DataFiles/Areas.xlsx]
(ooxml, embedded labels, table is Sheet1);

Customers:
LOAD
    "Unnamed: 0",
    MapStat_Val,
    ClosestBranchId,
    MainId,
    RouteWalkId,
    RouteCarId,
    MainFlag,
    MainPidtion,
    MainLong,
    MainLat,
    CarDistance,
    CarDuration,
    CarGeometry,
    WalkDistance,
    WalkDuration,
    WalkGeometry,
    ClosestWalkMin,
    ClosestGeometry,
    ClosestBranchName,
    ClosestPoint,
    ClosestFLag,
    CustomerID,
    FirstName,
    LastName,
    CustomerFullName,
    CustomerEmail,
    CustomerPhone,
    CustomerAddress,
    CustomerCity,
    Customer_PostalCode,
    CustomerCountry,
    NearestStoreID,
    NearestStoreName,
    CustomerLat,
    CustomerLong,
    DistanceToStoreKm,
    CustomerPostalCode
FROM [lib://DataFiles/Customers_HE_v2.xlsx]
(ooxml, embedded labels, table is לקוחות);

Shipments:
LOAD
    ShipmentID,
    SalesID,
    CarrierName,
    ShipDate,
    DeliveryDate,
    Status
FROM [lib://DataFiles/Shipments_UPDATED_v3_utf8.csv]
(txt, utf8, embedded labels, delimiter is ',', msq);


Items:
LOAD
    ItemID,
    ItemName,
    Brand,
    Category,
    Packaging,
    ItemSize,
    Variety,
    PackQuantity,
    GroupName,
    ProductGroup,
    Price,
    UnitCost
FROM [lib://DataFiles/Items.csv]
(txt, utf8, embedded labels, delimiter is ',', msq);

StoreTable:
LOAD
    StoreName,
    StoreID,
    Store_Name,
    Long,
    Lat,
    Stores_City,
    StorePoint,
    סניף
FROM [lib://DataFiles/StoreTable.xlsx]
(ooxml, embedded labels, table is Sheet1);

Employees:
LOAD
    EmployeeID,
    EmployeeName,
    EmployeeTitle,
    EmployeeBirthdate,
    HireDate,
    EmployeeAddress,
    EmployeeCity,
    EmployeePostalcode,
    EmployeeCountry,
    EmployeePhone,
    EmployeeStoreID,
    TenureYears,
    EmployeeName_Original
FROM [lib://DataFiles/Employees_unique_names_v2_utf8.csv]
(txt, utf8, embedded labels, delimiter is ',', msq);

// Goals:
// LOAD
//     Goals.TargetType,
//     EmployeeID,
//     Goals.GoalEmployeeStoreID,
//     Goals.StoreName,
//     Goals.City,
//     Goals.Year,
//     Goals.Quarter,
//     TargetQuarterDate,
//     Goals.TargetRevenue
// FROM [lib://DataFiles/Goals_UPDATED_v3_utf8.csv]
// (txt, utf8, embedded labels, delimiter is ',', msq);

///$tab Sales Fact TBL
$(Must_Include=[script://a799dd9f-ee90-4c33-8394-dd036ad251ce]);
///$tab Linkstore + Inventory Script
$(Must_Include=[script://a85bded1-3647-410f-8e0f-9c8621a555a8]);
///$tab 2026
// Future generated files: 2025-09-15 to 2026-12-31

CONCATENATE (Sales)
LOAD
    SalesLineID,
    SalesID,
    qty,
    unitpriceatsale,
    linetotal,
    LineChannel,
    LineIsStore,
    orderflag,
    lineshipping_once,
    OrderDateTime,
    HeaderDate,
    CustomerID,
    StoreID_Header,
    shippingfee,
    EmployeeID,
    unitcost,
    linemargin,
    linemarginpct,
    __ItemID,
    LinkKey,
    __Category
FROM [lib://Jonatan (Presale):DataFiles/Sales_FUTURE 25Q426.csv]
(txt, utf8, embedded labels, delimiter is ',', msq)
WHERE Floor(Date#(HeaderDate, 'YYYY-MM-DD')) <= $(vToday);


CONCATENATE (Shipments)
LOAD
    ShipmentID,
    SalesID,
    CarrierName,
    ShipDate,
    DeliveryDate,
    Status
FROM [lib://Jonatan (Presale):DataFiles/Shipments_FUTURE 25Q426.csv]
(txt, codepage is 28591, embedded labels, delimiter is ',', msq)
WHERE Floor(Date#(ShipDate, 'YYYY-MM-DD')) <= $(vToday);



// CONCATENATE (Goals)
// LOAD
//     Goals.TargetType,
//     EmployeeID,
//     Goals.GoalEmployeeStoreID,
//     Goals.StoreName,
//     Goals.City,
//     Goals.Year,
//     Goals.Quarter,
//     TargetQuarterDate,
//     Goals.TargetRevenue
// FROM [lib://Jonatan (Presale):DataFiles/Goals_FUTURE 25Q426.csv]
// (txt, utf8, embedded labels, delimiter is ',', msq)
// WHERE Floor(Date#(TargetQuarterDate, 'YYYY-MM-DD')) <= $(vToday);

CONCATENATE (Link_StoreItemDate)
LOAD
    LinkKey,
    StoreID,
    ItemID,
    Date_lk
FROM [lib://DataFiles/Link_StoreItemDate25Q426.qvd] (qvd)
WHERE Floor(Date_lk) <= $(vToday);


CONCATENATE (InventoryDaily)
LOAD
    LinkKey,
    OnHandQty
FROM [lib://DataFiles/InventoryDaily25Q426.qvd] (qvd)
WHERE Exists(LinkKey);





///$tab AutoCalendar
[autoCalendar]: 
  DECLARE FIELD DEFINITION Tagged ('$date')
FIELDS
  Dual(Year($1), YearStart($1)) AS [Year] Tagged ('$axis', '$year'),
  Dual('Q'&Num(Ceil(Num(Month($1))/3)),Num(Ceil(NUM(Month($1))/3),00)) AS [Quarter] Tagged ('$quarter', '$cyclic'),
  Dual(Year($1)&'-Q'&Num(Ceil(Num(Month($1))/3)),QuarterStart($1)) AS [YearQuarter] Tagged ('$yearquarter', '$qualified'),
  Dual('Q'&Num(Ceil(Num(Month($1))/3)),QuarterStart($1)) AS [_YearQuarter] Tagged ('$yearquarter', '$hidden', '$simplified'),
  Month($1) AS [Month] Tagged ('$month', '$cyclic'),
  Dual(Year($1)&'-'&Month($1), monthstart($1)) AS [YearMonth] Tagged ('$axis', '$yearmonth', '$qualified'),
  Dual(Month($1), monthstart($1)) AS [_YearMonth] Tagged ('$axis', '$yearmonth', '$simplified', '$hidden'),
  Dual('W'&Num(Week($1),00), Num(Week($1),00)) AS [Week] Tagged ('$weeknumber', '$cyclic'),
  Date(Floor($1)) AS [Date] Tagged ('$axis', '$date', '$qualified'),
  Date(Floor($1), 'D') AS [_Date] Tagged ('$axis', '$date', '$hidden', '$simplified'),
  If (DayNumberOfYear($1) <= DayNumberOfYear(Today()), 1, 0) AS [InYTD] ,
  Year(Today())-Year($1) AS [YearsAgo] ,
  If (DayNumberOfQuarter($1) <= DayNumberOfQuarter(Today()),1,0) AS [InQTD] ,
  4*Year(Today())+Ceil(Month(Today())/3)-4*Year($1)-Ceil(Month($1)/3) AS [QuartersAgo] ,
  Ceil(Month(Today())/3)-Ceil(Month($1)/3) AS [QuarterRelNo] ,
  If(Day($1)<=Day(Today()),1,0) AS [InMTD] ,
  12*Year(Today())+Month(Today())-12*Year($1)-Month($1) AS [MonthsAgo] ,
  Month(Today())-Month($1) AS [MonthRelNo] ,
  If(WeekDay($1)<=WeekDay(Today()),1,0) AS [InWTD] ,
  (WeekStart(Today())-WeekStart($1))/7 AS [WeeksAgo] ,
  Week(Today())-Week($1) AS [WeekRelNo] ;

DERIVE FIELDS FROM FIELDS [Date_lk], [HeaderDate]
USING [autoCalendar] ;


///$tab Exit Script
Exit Script;///$tab Distribution List
Replace LOAD
    "Name" AS [DL_DISTRIBUTION_RECIPIENT_NAME],
    "Email" AS [DL_DISTRIBUTION_EMAIL],
    SubField([Filters],',') AS [DL_DISTRIBUTION_FILTERS],
    SubField([Groups],',') AS [DL_DISTRIBUTION_GROUP_NAMES]
FROM "lib://:DataFiles/SampleFile3.xlsx"
(ooxml, embedded labels, table is DL_DISTRIBUTION_SVC_USERS_QCS);

TempGroups:
  REPLACE LOAD
      *
    FROM "lib://:DataFiles/SampleFile3.xlsx"
    (ooxml, embedded labels, table is DL_DISTRIBUTION_SVC_GROUPS_QCS);

let vEnabledFieldNo = FieldNumber('Enabled', 'TempGroups');

if (vEnabledFieldNo > 0) then
    DL_DISTRIBUTION_SVC_GROUPS_QCS:
    REPLACE LOAD
        "Name" AS [DL_DISTRIBUTION_GROUP_NAMES],
        "Description" AS [DL_DISTRIBUTION_GROUP_DESCRIPTION],
        "Enabled" AS [DL_DISTRIBUTION_GROUP_ENABLED]
    Resident TempGroups;
else

    DL_DISTRIBUTION_SVC_GROUPS_QCS:
    REPLACE LOAD
        "Name" AS [DL_DISTRIBUTION_GROUP_NAMES],
        "Description" AS [DL_DISTRIBUTION_GROUP_DESCRIPTION],
        1 AS [DL_DISTRIBUTION_GROUP_ENABLED]
    Resident TempGroups;
end if

Drop Table TempGroups;

tag field DL_DISTRIBUTION_RECIPIENT_NAME with 'DL_DISTRIBUTION_SVC__recipientName';
tag field DL_DISTRIBUTION_EMAIL with 'DL_DISTRIBUTION_SVC__recipientEmail';
tag field DL_DISTRIBUTION_FILTERS with 'DL_DISTRIBUTION_SVC__recipientFilters';
tag field DL_DISTRIBUTION_GROUP_NAMES with 'DL_DISTRIBUTION_SVC__groupsName';
tag field DL_DISTRIBUTION_GROUP_DESCRIPTION with 'DL_DISTRIBUTION_SVC__groupDescription';
tag field DL_DISTRIBUTION_GROUP_ENABLED with 'DL_DISTRIBUTION_SVC__groupEnabled';
